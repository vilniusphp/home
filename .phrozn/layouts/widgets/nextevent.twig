<h1>{{ entry.events.0.name }}</h1>

<div class="speakers row">
    {% for speaker in entry.events.0.speakers %}
        <div class="col-md-4">
            <div class="speaker well">
                <h2>{{ speaker.title }}</h2>
                {% if speaker.description %}
                    <div class="desciption">{{ speaker.description }}</div>
                {% endif %}
                {% if speaker.linkedin %}
                    <div class="name"> <a class="shadow-z-1" href="{{ speaker.linkedin }}"><img src="media/img/linkedin.png" alt="Linkedin: {{ speaker.name }}"/></a> {{ speaker.name }}</div>
                {% else %}
                    <div class="name">{{ speaker.name }}</div>
                {% endif %}
            </div>
        </div>
    {% else %}
        <div class="no-speakers well">
            Pranešėjai dar nepaskelbti. Nori pristatyti temą pats?
        </div>
    {% endfor %}
</div>

<div class="jumbotron">
    <div class="row">
        <div class="event-time-place {% if entry.events.0.sponsor %}col-md-4{% endif %}">
            <div class="date">
                <i class="mdi-action-event"></i> {{ entry.events.0.date }}
            </div>
            <div class="place">
                <i class="mdi-maps-place"></i>{{ entry.place.name }}, {{ entry.place.address }}
                <a href="{{ entry.place.mapurl }}" target="_blank"><i class="mdi-maps-map" title="žemėlapis"></i></a>
            </div>
            <div class="register">
                <a class="btn btn-large btn-success" href="{{ entry.events.0.facebook }}">Užsiregistruok!</a>
            </div>
        </div>

        {% if entry.events.0.sponsor %}
            <div class="sponsor col-md-8">
                <h3>Pagrindinis susitikimo rėmėjas</h3>
                {% for item in entry.events.0.sponsor %}
                    <a href="{{ item.url }}" title="{{ item.title }}"><img src="media/img/{{ item.logo }}" alt="{{ item.title }}" /></a>
                {% endfor %}
            </div>
        {% endif %}
    </div>
</div>